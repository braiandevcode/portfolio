// BACKEND
export enum EBackend {
  NODEJS = "Node.js",
  NESTJS = "Nestjs",
}

// DATABASE
export enum EDatabase {
  DB_PG = "PostgreSQL",
  DB_MDB = "MongoDB",
  DB_MSQL = "Mysql",
}

// FRONTEND
export enum EFrontend {
  REACT = "React",
  TS = "TypeScript",
  TCSS = "TailwindCSS",
  JS = "JavaScript",
}

// PERFORMANCE
export enum EPerformance {
  WEB_PERFORMANCE = "Web Performance",
  DEV_TOOL = "DevTools",
}

// CONTROL DE VERSIONES
export enum EVersionsControl {
  GIT = "Git",
  GIT_HUB = "GitHub",
}

// DISEÑO
export enum EDesign {
  FIGMA = "Figma",
  UI_UX = "UI/UX",
  CSS = "CSS",
}
